#include<stdio.h>
void main(){

    //multiply no. without using * 

    int n, multiply=0;
    printf("enter value of n ");
    scanf("%d", &n);

    multiply = n << 1;

    printf("n multiply by 2 is %d", multiply);

}